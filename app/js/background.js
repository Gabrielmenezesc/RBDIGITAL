/**
 * Descubra o Brasil - Chrome Extension Background Script
 * Handles background tasks if needed.
 */

chrome.runtime.onInstalled.addListener(() => {
    console.log("Descubra o Brasil Extension installed.");
});
